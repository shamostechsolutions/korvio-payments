services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: korvio
      POSTGRES_PASSWORD: korvio
      POSTGRES_DB: korvio
    ports:
      - "5434:5432"
    volumes:
      - korvio_pg:/var/lib/postgresql/data

volumes:
  korvio_pg:
